# API service environment template
# ------------------------------------------------------------------------------
# Local development defaults (non-secret placeholders). Replace these with values
# from your Supabase CLI sandbox or GitHub Actions environment secrets before
# running in staging/production. See docs/deployment/secrets-catalog.md for the
# authoritative mapping.
# ------------------------------------------------------------------------------

# Supabase (local sandbox)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PROJECT_REF=local-project-ref
SUPABASE_ANON_KEY=local-anon-key
SUPABASE_SERVICE_ROLE_KEY=local-service-role
SUPABASE_STORAGE_SIGNING_KEY=local-storage-signing

# Application crypto
API_JWT_SECRET=local-jwt-secret
JWT_SECRET=local-jwt-secret

# Google Cloud placeholders (use ADC or injected secrets in CI/CD)
CLOUD_RUN_REGION=asia-southeast1
GCP_PROJECT_ID=cb-api-local
GCLOUD_SERVICE_ACCOUNT_JSON="{\"type\":\"service_account\",\"project_id\":\"cb-api-local\"}"

# Downstream service endpoints
OCR_URL=http://127.0.0.1:9000
ALLOW_ORIGINS=["http://localhost:3000","exp://*"]
MAX_IMAGE_MB=15
TIMEOUT_MS=15000
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Reference (do not commit real secrets)
# Staging:
#   SUPABASE_URL=https://container-base-stg.supabase.co
#   SUPABASE_PROJECT_REF=container-base-stg
#   API_JWT_SECRET=stg-jwt-secret
# Production:
#   SUPABASE_URL=https://container-base.supabase.co
#   SUPABASE_PROJECT_REF=container-base-prod
#   API_JWT_SECRET=prod-jwt-secret
# ------------------------------------------------------------------------------
