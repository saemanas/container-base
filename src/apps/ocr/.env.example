# OCR worker environment template
# ------------------------------------------------------------------------------
# Local development defaults. Replace these before deploying to staging or
# production; consult docs/deployment/secrets-catalog.md for the canonical values.
# ------------------------------------------------------------------------------

# Supabase connectivity
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PROJECT_REF=local-project-ref
SUPABASE_ANON_KEY=local-anon-key
SUPABASE_SERVICE_ROLE_KEY=local-service-role

# Upstream API endpoint
API_BASE_URL=http://127.0.0.1:8000

# Worker tuning
MAX_IMAGE_MB=15
OCR_TIMEOUT_MS=15000
OCR_BATCH_SIZE=1
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Reference (do not commit real secrets)
# Staging:
#   SUPABASE_URL=https://container-base-stg.supabase.co
#   API_BASE_URL=https://api-stg.container-base.com
# Production:
#   SUPABASE_URL=https://container-base.supabase.co
#   API_BASE_URL=https://api.container-base.com
# ------------------------------------------------------------------------------
